name: example
description: A example tus client Flutter project.

publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  tus_client_dart:
    path: ../
  cross_file: ^0.3.3+5
  file_picker: ^4.6.1
  url_launcher: ^6.1.14
  path_provider: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
